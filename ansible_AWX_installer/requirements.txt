# Pinned Python dependencies for the AWX installer control environment (.venv).
#
# Installed by scripts/bootstrap-host.sh into ansible_AWX_installer/.venv .
# Never installed globally and never with sudo.
#
# Exact versions are reconciled to what pip actually resolves at bootstrap time
# (see resolve-versions.sh / docs/test-results.md). If a pin below is not
# available on PyPI, bootstrap reports it clearly rather than silently floating.

ansible-core==2.18.6
ansible-lint==24.12.2
yamllint==1.35.1
jmespath==1.0.1
